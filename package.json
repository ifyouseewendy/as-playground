{
  "devDependencies": {
    "@shopify/scripts-toolchain-as": "^1.1.0",
    "assemblyscript": "^0.14.0",
    "as-wasi": "^0.2.1"
  },
  "scripts": {
    "asbuild": "npx shopify-scripts-build --src=src/script.ts --binary=build/script.wasm --schema=schema -- --lib=../node_modules --optimize --use Date=",
    "test": "node tests"
  },
  "engines": {
    "node": ">=14.5"
  }
}
