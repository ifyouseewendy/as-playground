{
  "devDependencies": {
    "@shopify/scripts-toolchain-as": "file:~/scripts-toolchain-as",
    "assemblyscript": "^0.14.0",
    "as-wasi": "^0.2.1",
    "as-msgpack": "wapc/as-msgpack",
    "@as-pect/cli": "^4.0.0"
  },
  "scripts": {
    "asbuild": "npx shopify-scripts-toolchain-as build --src=src/script.ts --binary=build/script.wasm --schema=schema -- --lib=../node_modules --optimize --use Date=",
    "test": "asp --config tests/as-pect.config.js --verbose --summary"
  },
  "engines": {
    "node": ">=14.5"
  }
}
